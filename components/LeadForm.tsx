"use client";import {FormEvent,useState} from "react";import {submitToNalarva} from "@/lib/apps-script";
export default function LeadForm({mode="contact"}:{mode?:"contact"|"register"}){const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const fd=new FormData(e.currentTarget);const data=Object.fromEntries(fd.entries());const r=await submitToNalarva(mode,data);setMsg(r.message|| (r.ok?"Data berhasil dikirim.":"Terjadi kesalahan."));setBusy(false)}return <form className="form-card" onSubmit={submit}><label>Nama siswa<input name="name" required/></label>{mode==="register"&&<label>Email<input name="email" type="email" required/></label>}<label>Nomor WhatsApp<input name="phone" required placeholder="08xxxxxxxxxx"/></label><label>Jenjang<select name="level"><option>TKA SD</option><option>TKA SMP</option><option>TKA SMA</option></select></label>{mode==="contact"?<label>Pesan<textarea name="message" placeholder="Ceritakan kebutuhan belajarnya..."/></label>:<label>Sekolah<input name="school"/></label>}<button className="btn primary wide" disabled={busy}>{busy?"Mengirim...":mode==="register"?"Kirim Pendaftaran":"Kirim Pertanyaan"}</button>{msg&&<p className="form-message">{msg}</p>}</form>}
